% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mstp_capacity_optimize}
\alias{mstp_capacity_optimize}
\title{Optimise carrier capacities: LP proxy vs SDDP dual gradient (CRN)}
\usage{
mstp_capacity_optimize(
  config,
  v,
  outer = 12L,
  cold = 100L,
  eval_iters = 300L,
  n_eval = 500L,
  n_capdual = 200L,
  seed = NULL
)
}
\arguments{
\item{config}{\code{mstp_config} object.}

\item{v}{Per-unit reservation price (e.g. \verb{0.2 * mean(transport rate)}).}

\item{outer}{Outer projected-gradient iterations (default 12).}

\item{cold}{SDDP iterations per gradient step for the SDDP-dual source
(cold start; default 100).}

\item{eval_iters}{SDDP iterations for the out-of-sample scoring policy
(default 300).}

\item{n_eval}{Shared out-of-sample scenarios for scoring (default 500).}

\item{n_capdual}{Trajectories used to average the SDDP capacity duals
(default 200).}

\item{seed}{Optional integer base seed (derived sub-streams).}
}
\value{
A named list: total procurement costs (\code{x0_total}, \code{lp_total},
\code{sddp_total}), percentage change vs default (\code{lp_pct}, \code{sddp_pct}), the SDDP
advantage in percentage points (\code{adv_pp}), and mean capacities per plan.
}
\description{
Runs the capacity-optimisation comparison in the Julia core: projected
gradient descent from the default capacities using (a) the duals of a
mean-demand LP proxy and (b) the SDDP stage capacity duals, then scores all
three capacity vectors (default, LP proxy, SDDP) on a shared out-of-sample
scenario set (common random numbers). All scenarios are sampled in R, so the
result is reproducible for a given \code{seed}.
}
