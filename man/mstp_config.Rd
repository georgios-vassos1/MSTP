% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mstp_config}
\alias{mstp_config}
\title{Build an MSTP configuration (HyperParams) from an instance list}
\usage{
mstp_config(instance, lambda = 2000, corrmat = NULL, n_scenarios = 20L)
}
\arguments{
\item{instance}{Named list as returned by \code{generate_instance()}.}

\item{lambda}{Poisson intensity for the uncertainty model. Either a
single numeric (broadcast to all flow dimensions) or a
numeric vector of length \code{nOrigins + nDestinations}.}

\item{corrmat}{Correlation matrix of size \code{(nOrigins+nDestinations)^2}.
Use \code{mstp_corrmat()} to generate one.}

\item{n_scenarios}{Number of training scenarios (support size per stage).}
}
\value{
An object of class \code{mstp_config}: a list with the opaque Julia proxy
\code{jl} (the \code{HyperParams}) plus the R-side sampling parameters (\code{tau}, \code{dim},
\code{lambda}, \code{corrmat}, \code{n_scenarios}) that \code{\link[=mstp_train]{mstp_train()}} and \code{\link[=mstp_simulate]{mstp_simulate()}}
use to draw scenarios in R.
}
\description{
Passes instance parameters to Julia and returns an opaque Julia proxy
representing the \code{HyperParams} struct. The proxy can be passed to
\code{mstp_train()} and is otherwise invisible to the user.
}
