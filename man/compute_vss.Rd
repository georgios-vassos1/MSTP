% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recourse.R
\name{compute_vss}
\alias{compute_vss}
\title{Compute gain of recourse: SDDP vs myopic policy}
\usage{
compute_vss(sims, sddp_results, n_cores = 1L)
}
\arguments{
\item{sims}{List of instances (from \code{\link[=generate_instance]{generate_instance()}} or
\code{\link[=load_instances]{load_instances()}}).}

\item{sddp_results}{List of \code{\link[=mstp_simulate]{mstp_simulate()}} result lists, one per instance.}

\item{n_cores}{Number of parallel workers (default 1 = serial).}
}
\value{
Numeric matrix of shape \verb{(n_trials x n_instances)} of gain values.
}
\description{
For each instance and each out-of-sample trajectory from \code{\link[=mstp_simulate]{mstp_simulate()}},
rolls a myopic (greedy single-stage) policy forward on the same realised noise
and compares its cost against the SDDP policy cost. Both costs use the SDDP
stage accounting (inst/julia/model.jl), so the comparison is like-for-like.
}
\details{
Gain of recourse \eqn{= (\mathrm{myopic} - \mathrm{SDDP}) / \mathrm{myopic}}.
}
