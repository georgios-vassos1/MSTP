% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproduce.R
\name{mstp_reproduce_recourse}
\alias{mstp_reproduce_recourse}
\title{Reproduce the recourse results (regret and gain of recourse)}
\usage{
mstp_reproduce_recourse(spec, seed = 1L)
}
\arguments{
\item{spec}{A named list as in \code{\link[=mstp_reproduce_bounds]{mstp_reproduce_bounds()}} (\code{tau}, \code{nOrigins},
\code{nDestinations}, \code{nCarriers}, \code{lambda}, \code{iters}, optionally \code{label},
\code{trials}, \code{n_scenarios}, \code{rho_oo}, \code{rho_dd}, \code{rho_cross}).}

\item{seed}{Integer base seed (reproduces the whole row).}
}
\value{
A one-row \code{data.frame} with \code{label}, \code{mean_regret_pct},
\code{median_regret_pct}, \code{q95_regret_pct}, and \code{mean_gain_pct}.
}
\description{
Generates a seed-deterministic instance, trains an SDDP policy, simulates it
out-of-sample, and reports regret against the perfect-foresight LP
(Table 2 in the paper) and gain of recourse against the myopic policy. All
analysis is pure R (no TLPR).
}
