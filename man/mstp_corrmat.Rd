% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{mstp_corrmat}
\alias{mstp_corrmat}
\title{Block-structured Gaussian-copula correlation matrix}
\usage{
mstp_corrmat(
  nOrigins,
  nDestinations,
  rho_oo = 0.6,
  rho_dd = 0.7,
  rho_cross = 0
)
}
\arguments{
\item{nOrigins}{Integer >= 1. Number of origin (entry) locations.}

\item{nDestinations}{Integer >= 1. Number of destination (exit) locations.}

\item{rho_oo}{Numeric in [0, 1). Origin-origin off-diagonal correlation.}

\item{rho_dd}{Numeric in [0, 1). Destination-destination off-diagonal correlation.}

\item{rho_cross}{Numeric in [0, 1). Origin-destination (cross-block) correlation.}
}
\value{
A symmetric \verb{d x d} numeric matrix (\code{d = nOrigins + nDestinations})
with unit diagonal. Verified positive definite before return.
}
\description{
Builds the \code{(nOrigins + nDestinations)}-dimensional correlation matrix used by
the copula sampler. The origin block carries a common off-diagonal
correlation \code{rho_oo}, the destination block \code{rho_dd}, and every origin-vs-
destination (cross) entry \code{rho_cross}. The diagonal is 1.
}
\details{
This is the single source of truth for the correlation structure. Unlike the
legacy Julia \code{gen_cov_mat()} (inst/julia/utils.jl:10), the within-block values
are explicit arguments rather than random draws, matching the fixed values
documented in the paper (rho_OO = 0.6, rho_DD = 0.7).
}
