% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{mstp_scenario_support}
\alias{mstp_scenario_support}
\title{Per-stage in-sample support for SDDP training}
\usage{
mstp_scenario_support(tau, n_scenarios, lambda, corrmat, seed = NULL)
}
\arguments{
\item{tau}{Integer >= 1. Number of stages (planning horizon).}

\item{n_scenarios}{Integer >= 1. Support size per stage.}

\item{lambda, corrmat}{As in \code{\link[=mstp_sample_copula]{mstp_sample_copula()}}.}

\item{seed}{Optional integer seed (scoped; see \code{\link[=mstp_sample_copula]{mstp_sample_copula()}}).}
}
\value{
A list of length \code{tau}; element \code{t} is an \verb{n_scenarios x d} integer
matrix of noise outcomes for stage \code{t}. Equal probabilities are implied.
}
\description{
Builds the finite noise support Omega that the SDDP stage subproblems are
parameterised over. Under stagewise independence each of the \code{tau} stages
gets an independent draw of \code{n_scenarios} outcomes, reproducing the per-stage
sampling in inst/julia/model.jl:48 but with the draws owned by R.
}
