% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mstp_train_warm}
\alias{mstp_train_warm}
\title{Train an SDDP policy with a warm start from existing cuts}
\usage{
mstp_train_warm(config, iterations, cuts_path, seed = NULL)
}
\arguments{
\item{config}{\code{mstp_config} object returned by \code{mstp_config()}.}

\item{iterations}{Additional SDDP iterations to run after loading cuts.}

\item{cuts_path}{Path to a cuts file written by \code{mstp_write_cuts()}.}

\item{seed}{Optional integer seed for the scenarios drawn in R
(support and forward trajectories).}
}
\value{
An opaque Julia proxy (SDDP.PolicyGraph).
}
\description{
Builds a new \code{PolicyGraph} from \code{config}, loads Benders cuts from
\code{cuts_path}, then continues training. Because cuts are functions of the
inventory state variables (not of \code{carrier_capacity}), they transfer
correctly across capacity changes and dramatically reduce the iterations
needed for the lower bound to tighten.
}
