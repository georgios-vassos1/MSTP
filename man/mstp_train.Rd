% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mstp_train}
\alias{mstp_train}
\title{Train an SDDP policy}
\usage{
mstp_train(config, iterations = 1500L, seed = NULL)
}
\arguments{
\item{config}{\code{mstp_config} object returned by \code{mstp_config()}.}

\item{iterations}{Number of SDDP training iterations (default 1500). One
forward trajectory is drawn per iteration.}

\item{seed}{Optional integer. When supplied, the per-stage support and
the forward trajectories are drawn reproducibly (from derived sub-streams
\code{seed} and \code{seed + 1}), so the trained policy — and its lower bound — are
deterministic.}
}
\value{
An opaque Julia proxy (SDDP.PolicyGraph).
}
\description{
Builds and trains the multistage stochastic transportation model using
SDDP. Returns an opaque Julia proxy for the trained \code{PolicyGraph}, which
can be passed to \code{mstp_simulate()}.
}
