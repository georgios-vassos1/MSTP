% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mstp_capacity_duals}
\alias{mstp_capacity_duals}
\title{Extract expected carrier capacity duals from a trained SDDP model}
\usage{
mstp_capacity_duals(model, config, n_samples = 100L, seed = NULL)
}
\arguments{
\item{model}{Julia proxy returned by \code{mstp_train()}.}

\item{config}{\code{mstp_config} object returned by \code{mstp_config()}.}

\item{n_samples}{Number of simulation trajectories used to average the duals.}

\item{seed}{Optional integer for reproducible trajectories (derived
sub-stream \code{seed + 4}).}
}
\value{
Numeric vector of length \code{(nCarriers + nSpotCarriers) * tau}.
}
\description{
Simulates \code{n_samples} out-of-sample trajectories (drawn in R) and averages
the dual variable of each carrier capacity constraint at every stage. The
returned vector has the same flat layout as \code{instance$carrier_capacity} —
index \code{(k-1)*tau + t} for carrier \code{k} at stage \code{t} — so it can be used
directly as the gradient \verb{∂V/∂carrier_capacity} in a capacity optimisation
loop.
}
\details{
Because duals of a minimisation \code{<=} constraint are non-positive, the
gradient of \verb{V(x) + v·x} with respect to \code{x} is \code{cap_duals + v}.
}
