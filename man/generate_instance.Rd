% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geninst.R
\name{generate_instance}
\alias{generate_instance}
\title{Generate a random MSTP instance}
\usage{
generate_instance(
  tau = 12L,
  nOrigins = 6L,
  nDestinations = 6L,
  nCarriers = 20L,
  nSpotCarriers = nCarriers,
  carriers_per_lane = 2L,
  seed = NULL,
  lambda = 700,
  target_util = 0.8,
  util_lo = 0.5,
  util_hi = 1.5,
  rate_lo = 6,
  rate_hi = 8,
  store_periods = 20,
  init_stock = 0L,
  entry_capacity = NULL,
  exit_capacity = NULL
)
}
\arguments{
\item{tau}{Number of time periods.}

\item{nOrigins}{Number of origin nodes.}

\item{nDestinations}{Number of destination nodes.}

\item{nCarriers}{Number of contracted carriers.}

\item{nSpotCarriers}{Number of spot carriers (defaults to \code{nCarriers}).}

\item{carriers_per_lane}{Number of contracted carriers awarded each lane
(default 2). Keep small (1-3) to avoid LP degeneracy; clamped to
\verb{[1, nCarriers]}.}

\item{seed}{Optional integer random seed for reproducibility.}

\item{lambda}{Expected demand per origin per period, used to
calibrate carrier capacities. Defaults to 700.}

\item{target_util}{Target mean utilisation for contracted carriers.}

\item{util_lo, util_hi}{Bounds of the per-carrier utilisation multiplier.}

\item{rate_lo, rate_hi}{Range over which distinct contracted base rates are
spaced (spot rates are drawn above \code{rate_hi}).}

\item{store_periods}{Storage capacity expressed as this many periods of
demand (\code{ceil(store_periods * lambda)}). Default 20 keeps the buffer
non-binding without the absurdly large (e.g. 1e6) values that wreck the LP
right-hand-side conditioning.}

\item{init_stock}{Deterministic initial inventory at every node (default
0, i.e. an empty system). Must NOT be randomised: random per-node initial
stock makes the long-horizon SDDP lower bound numerically invalid.}

\item{entry_capacity, exit_capacity}{Optional explicit storage caps (override
the \code{store_periods} default).}
}
\value{
A named list suitable for \code{mstp_config()}.
}
\description{
Produces a named list representing one problem instance: network structure
(awarded lanes per carrier), initial stocks, capacities, and cost
coefficients. The list can be passed directly to \code{mstp_config()}.
}
\details{
Carrier structure (important for numerical stability). Each origin-destination
lane is awarded to a SMALL number of carriers (\code{carriers_per_lane}, default 2)
with DISTINCT base rates spaced across \verb{[rate_lo, rate_hi]}. This mirrors real
drayage — a lane is served by a few rate-differentiated carriers — and, unlike
assigning many near-identical carriers to every lane, it avoids the massive
primal degeneracy that produces unstable LP duals, ghost Benders-cut slopes,
and an invalid SDDP lower bound at long horizons / large instances. The total
number of carriers can still be large (scalability); only the per-lane overlap
is kept small.

Carrier capacities are calibrated to \code{lambda} so the average contracted
carrier operates at roughly \code{target_util} utilisation (default 80\%).
Spot carriers are priced above contracted rates and given finite capacity for
overflow relief.
}
