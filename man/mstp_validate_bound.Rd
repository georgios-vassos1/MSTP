% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mstp_validate_bound}
\alias{mstp_validate_bound}
\title{Check whether the SDDP lower bound is numerically valid}
\usage{
mstp_validate_bound(model, config, trials = 200L, z = 3, seed = NULL)
}
\arguments{
\item{model}{Julia proxy returned by \code{mstp_train()}.}

\item{config}{\code{mstp_config} object returned by \code{mstp_config()}.}

\item{trials}{Number of out-of-sample simulations (default 200).}

\item{z}{Standard-error margin allowed before flagging (default 3).}

\item{seed}{Optional integer for reproducible evaluation scenarios.}
}
\value{
A named list: \code{bound}, \code{sim_mean}, \code{sim_se},
\code{margin_se} (SEs the bound sits above the mean; positive = overshoot),
\code{trials}, and \code{valid} (logical).
}
\description{
A valid SDDP lower bound for a minimisation satisfies
\code{bound <= E[policy cost]}. This retrieves the lower bound, runs an
out-of-sample simulation to estimate \code{E[policy cost]}, and applies the
R decision rule \code{\link[=mstp_bound_validity]{mstp_bound_validity()}} (valid iff the bound does not exceed
the simulated mean by more than \code{z} standard errors). Use it to detect
the ghost-cut overshoot (LB > UB) that arises from ill-conditioned LP duals
on long horizons / large instances, so an invalid bound is never reported
silently.
}
