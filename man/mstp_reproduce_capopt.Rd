% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproduce.R
\name{mstp_reproduce_capopt}
\alias{mstp_reproduce_capopt}
\title{Reproduce a capacity-optimisation regime (Tables 7-8)}
\usage{
mstp_reproduce_capopt(spec, seed = 1L)
}
\arguments{
\item{spec}{Named list: \code{tau}, \code{nOrigins}, \code{nDestinations}, \code{nCarriers},
\code{lambda}, and optionally \code{label}, \code{target_util} (default 0.8), \code{n_scenarios}
(20), \code{rho_cross} (0.0), \code{rho_oo} (0.6), \code{rho_dd} (0.7), and the optimisation
controls \code{outer} (12), \code{cold} (100), \code{eval_iters} (300), \code{n_eval} (500),
\code{n_capdual} (200).}

\item{seed}{Integer base seed.}
}
\value{
A one-row \code{data.frame}: \code{label}, \code{lp_pct}, \code{sddp_pct}, \code{adv_pp},
\code{x0_mean}, \code{xlp_mean}, \code{xsd_mean}.
}
\description{
Generates a seed-deterministic instance at a given demand/utilisation regime
and runs \code{\link[=mstp_capacity_optimize]{mstp_capacity_optimize()}}. The reservation price is fixed at
\verb{0.2 * mean(contract transport rate)}, matching the paper.
}
