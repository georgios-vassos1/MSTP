% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{sensitivity_inflow}
\alias{sensitivity_inflow}
\title{Inflow sensitivity of the clairvoyant cost}
\usage{
sensitivity_inflow(
  sims,
  flow_support = seq(20L, 40L, by = 5L),
  demand = NULL,
  n_samples = 200L,
  seed = NULL,
  n_cores = 1L
)
}
\arguments{
\item{sims}{List of instances (from \code{\link[=generate_instance]{generate_instance()}}/\code{\link[=load_instances]{load_instances()}}).}

\item{flow_support}{Integer vector — the per-node inflow sample space.}

\item{demand}{Constant per-node outflow while inflow varies. Default:
\code{round(mean(flow_support))}.}

\item{n_samples}{Number of inflow draws per instance.}

\item{seed}{Optional integer seed (scoped; global RNG is preserved).}

\item{n_cores}{Parallel workers (default 1 = serial).}
}
\value{
Numeric matrix \verb{(n_samples x n_instances)} of clairvoyant costs.
}
\description{
For each instance, draws \code{n_samples} inflow trajectories from \code{flow_support}
(outflow held at \code{demand}) and returns the perfect-foresight LP cost of each.
}
