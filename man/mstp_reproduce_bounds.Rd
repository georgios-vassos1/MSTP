% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproduce.R
\name{mstp_reproduce_bounds}
\alias{mstp_reproduce_bounds}
\title{Reproduce SDDP bound tables from a list of experiment specifications}
\usage{
mstp_reproduce_bounds(specs, seed = 1L, z = 3)
}
\arguments{
\item{specs}{A list of specifications. Each element is a named list with:
\code{tau}, \code{nOrigins}, \code{nDestinations}, \code{nCarriers}, \code{lambda}, \code{iters}, and
optionally \code{label}, \code{trials} (default 200), \code{n_scenarios} (default 10),
\code{rho_oo} (0.6), \code{rho_dd} (0.7), \code{rho_cross} (0.0), and \code{spot_mult} (1) —
a multiplier applied to the instance spot-cost coefficients.}

\item{seed}{Integer base seed; the same seed reproduces the whole table.}

\item{z}{Standard-error margin for the bound-validity flag (default 3).}
}
\value{
A \code{data.frame} with columns \code{label}, \code{tau}, \code{iters}, \code{LB}, \code{UB},
\code{gap_pct} (= 100*(UB-LB)/|LB|), \code{sim_se}, \code{margin_se}, \code{valid}.
}
\description{
For each spec, generates a seed-deterministic instance, builds the copula
correlation matrix, trains an SDDP policy (seeded), and certifies the lower
bound against a fresh out-of-sample estimate of the policy cost. Returns one
row per spec with the lower bound, out-of-sample mean (upper bound), certified
gap, and validity flag.
}
