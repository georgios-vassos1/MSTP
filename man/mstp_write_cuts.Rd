% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mstp_write_cuts}
\alias{mstp_write_cuts}
\title{Write SDDP Benders cuts to a file}
\usage{
mstp_write_cuts(model, path)
}
\arguments{
\item{model}{Julia proxy returned by \code{mstp_train()} or \code{mstp_train_warm()}.}

\item{path}{File path for the cuts JSON (created or overwritten).}
}
\value{
\code{path}, invisibly.
}
\description{
Persists the cuts from a trained model so they can be reloaded into a new
model via \code{mstp_train_warm()}. Cuts are keyed by stage and state-variable
names, so they remain valid when only \code{carrier_capacity} changes between
calls (the inventory state variables are unchanged).
}
