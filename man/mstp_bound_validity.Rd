% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bound_validity.R
\name{mstp_bound_validity}
\alias{mstp_bound_validity}
\title{Decide whether an SDDP lower bound is numerically valid}
\usage{
mstp_bound_validity(bound, costs, z = 3)
}
\arguments{
\item{bound}{Numeric scalar. The SDDP lower bound.}

\item{costs}{Numeric vector (length >= 1) of realised out-of-sample policy
costs.}

\item{z}{Numeric. Standard-error margin allowed before flagging (default 3).}
}
\value{
A named list:
\describe{
\item{mean}{Sample mean of \code{costs}.}
\item{se}{Standard error of the mean (sample sd / sqrt(n)).}
\item{margin_se}{Standard errors the bound sits above the mean; positive
means overshoot. \code{Inf}/\code{-Inf} when \code{se == 0} and the bound differs from
the mean, \code{0} when they are equal.}
\item{n}{Number of cost samples.}
\item{valid}{\code{TRUE} iff \code{bound <= mean + z * se}.}
}
}
\description{
Pure decision rule: given a lower \code{bound} and a sample of realised policy
\code{costs}, decide whether \code{bound <= E[cost]} within Monte-Carlo noise. A valid
bound must not exceed the sample mean by more than \code{z} standard errors.
}
