% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mstp_simulate}
\alias{mstp_simulate}
\title{Simulate the trained policy out-of-bag}
\usage{
mstp_simulate(model, config, trials = 1000L, seed = NULL)
}
\arguments{
\item{model}{Julia proxy returned by \code{mstp_train()}.}

\item{config}{\code{mstp_config} object returned by \code{mstp_config()}.}

\item{trials}{Number of OOB simulation replications (default 1000).}

\item{seed}{Optional integer for reproducible OOB scenarios (derived
sub-stream \code{seed + 2}).}
}
\value{
A named list:
\describe{
\item{obj}{Numeric vector of length \code{trials} — total cost per run.}
\item{noise}{Matrix \verb{(trials*tau) × (nOrigins+nDestinations)} —
realised inflows/outflows.}
\item{entry}{Matrix \verb{(trials*tau) × nOrigins} — entry inventory.}
\item{exitp}{Matrix \verb{(trials*tau) × nDestinations} — exit stock.}
\item{exitm}{Matrix \verb{(trials*tau) × nDestinations} — exit shortage.}
\item{moves}{Matrix \verb{(trials*tau) × (nLanes+nSpotLanes)} — allocations.}
\item{tau, trials, nOrigins, nDestinations, nLanes, nSpotLanes}{Dims.}
}
}
\description{
Draws \code{trials} out-of-sample scenario paths in R (reproducible when \code{seed} is
supplied) and runs the trained SDDP policy through them. Returns an R list
with objective values, noise realisations, inventory trajectories, and
carrier allocations — all as plain numeric matrices ready for analysis.
}
