% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{mstp_sample_copula}
\alias{mstp_sample_copula}
\title{Sample correlated-Poisson scenario vectors (Gaussian copula)}
\usage{
mstp_sample_copula(n, lambda, corrmat, seed = NULL)
}
\arguments{
\item{n}{Integer >= 1. Number of scenario vectors to draw.}

\item{lambda}{Numeric, length 1 or \code{d}. Poisson rate(s); length-1 is recycled
to all \code{d} dimensions. All entries must be > 0.}

\item{corrmat}{Symmetric \verb{d x d} correlation matrix (see \code{\link[=mstp_corrmat]{mstp_corrmat()}}).}

\item{seed}{Optional integer. If supplied, draws are seeded and the caller's
global RNG stream is left untouched (see Details).}
}
\value{
An integer matrix with \code{n} rows and \code{d} columns. Column order is
\verb{[inflow_1..nOrigins, outflow_1..nDestinations]}, matching
inst/julia/model.jl:50-51. Entries are non-negative.
}
\description{
Draws \code{n} independent scenario vectors from a Gaussian copula with Poisson
marginals. Mirrors \code{sample_scenarios()} in inst/julia/utils.jl:24-26.
}
\details{
Randomness is scoped: with \code{seed} non-NULL the function saves,
sets, and restores \code{.Random.seed}, so repeated calls with the same seed are
identical and no global RNG side effect leaks to the caller.
}
